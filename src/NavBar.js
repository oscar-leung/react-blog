import { Link } from "react-router-dom"

const NavBar = () => {
    return (
        <nav>
            <div>
                <button>Home</button>
                <button>About Me</button>
                <button>Pictures</button>
            </div>
        </nav>
    )
}

export default NavBar;